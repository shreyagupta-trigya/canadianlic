import React from "react";
import { Outlet } from "react-router-dom";

const ShipmentsTrackerLayout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default ShipmentsTrackerLayout;
